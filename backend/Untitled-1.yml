docker logs backend-ai-service           >logs
docker logs backend-ai-service           >logs
docker logs backend-aUth-service           >logs
docker logs backend-management-service   >logs
docker logs backend-billing-service      >logs
docker logs mysql   >logs

docker compose up -d
docker compose up -d mysql
docker compose up -d backend-autH-service          
docker compose up -d backend-ai-service          
docker compose up -d backend-management-service  
docker compose up -d backend-billing-service     